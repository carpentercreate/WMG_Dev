import React, {useState, useEffect} from "react";
//const useDoc = (id, parentRef) => {
//	const docref = parentRef.doc(id);
//	const doc = useFirestoreDocData(docref);
//	const {FieldValue} = useFirestore;
//	const set = docref.set;
//	const update = docref.update;
//	const remove = (key) => update({[key]: FieldValue.delete()});
//	return [doc, {docref, set, update, remove}];
//};
